import { API_BASE_URL as LOCAL_API_BASE_URL } from './local';
import { API_BASE_URL as LIVE_API_BASE_URL } from './live';

// Flip this to false when you want the web frontend to talk to your local backend.
const USE_LIVE_API = true;

export const API_BASE_URL = '';
